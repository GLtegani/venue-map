import { Archivo_Narrow, Pragati_Narrow } from "next/font/google";

export const archivo_narrow = Archivo_Narrow({
  subsets: ["latin"],
});

export const pragati_narrow = Pragati_Narrow({
  subsets: ["latin"],
  weight: ["400"],
});
